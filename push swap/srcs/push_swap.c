
#include "push_swap"

int main (int argc, char **argv)
{
    t_fill *created_el;

    if (argc < 2)
        exit(1);
    created_ = (t_fill *)malloc(sizeof(t_fill)))
    if (create == NULL)
        exit(1);

}

